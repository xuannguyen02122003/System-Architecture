# Backend dependencies for the AI Bookstore Assistant
# Kept intentionally small — this is a proof-of-concept.

fastapi==0.115.6          # web framework (async, great for SSE streaming)
uvicorn[standard]==0.34.0 # ASGI server to run FastAPI
openai==1.59.6            # OpenAI-compatible LLM client (swappable provider)
python-dotenv==1.0.1      # loads the .env file (API key, model name)
pypdf==5.1.0             # read text out of PDF policy documents
pytest==8.3.4            # test runner for the retrieval layer
